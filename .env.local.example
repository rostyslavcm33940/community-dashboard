# Copy this file to .env.local and fill in your values.
# .env.local is git-ignored — never commit your secrets.

# From Supabase: Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://ztxtoaogbbpkkvcqvorq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only (writes). NEVER expose with NEXT_PUBLIC_ prefix.
SUPABASE_SERVICE_ROLE_KEY=
